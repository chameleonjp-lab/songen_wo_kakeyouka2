# 尊厳を賭けようか2 仕様書

更新日: 2026-06-23  
対象ファイル: `index.html`  
公開想定URL: `https://chameleonjp.codeberg.page/songen_wo_kakeyouka2/`

この文書は、ブラウザゲーム「尊厳を賭けようか2」の正式な仕様書です。実装を変更する場合は、この仕様と矛盾しないようにします。

---

## 1. ゲーム概要

「尊厳を賭けようか2」は、前作の1戦型バトルを土台にした、最大5戦の連戦ゲームです。

プレイヤーはCPUを順番に倒していきます。第1戦から第4戦までは、勝利後に報酬を選びます。第5戦が終わると、勝敗にかかわらず総合結果へ進みます。

このゲームの中心は、壊れた体を次の戦闘へ持ち越すことです。腕、足、頭が壊れても完全には詰まないように、後ろ腕の前出し、足破壊時の減速移動、両腕なし頭攻撃を許可します。

---

## 2. 基本定数

### 2.1 GAME

```js
const GAME = Object.freeze({
  title: "尊厳を賭けようか2",
  slug: "songen_wo_kakeyouka2",
  url: "https://chameleonjp.codeberg.page/songen_wo_kakeyouka2/",
  labUrl: "https://chameleonjp.codeberg.page/chameleonjp_lab/",
  durationSec: 180,
  version: "songen_wo_kakeyouka2_v1_20260623_01_five_battles_head_attack"
});
```

`GAME.slug` はランキング送信時のゲーム識別子として使います。前作とは必ず分けます。

### 2.2 連戦定数

```js
const RUN_MAX_BATTLES = 5;
const REWARD_SCORE = 1000;
const RUN_ENEMIES = ["徘徊者", "コメ職人", "荒らし", "モデレーター", "尊厳の管理人"];
```

---

## 3. 画面構成

画面は次の構成です。

```text
homeScreen      ホーム画面
rulesModal      ルール説明
setupScreen     試合前設定
battleScreen    戦闘画面
outcomeScreen   1戦ごとの決着画面
rewardScreen    報酬選択画面
resultScreen    総合結果画面
```

### 3.1 ホーム画面

表示文言:

```text
尊厳を賭けようか2

5人抜きで尊厳を守れ。
壊れた体を引きずって、次の相手へ進め。
```

ボタン:

```text
ゲーム開始
ゲームをシェア
ルール説明
他のゲームで遊ぶ
```

### 3.2 試合前設定

設定できる項目は次の通りです。

```text
プレイヤー名
プレイヤー頭選択
CPU頭選択
プレイヤーの位置
```

CPU名入力欄は置きません。敵名は5人抜きの固定順で決まります。

### 3.3 戦闘画面

戦闘画面のHUDには次を表示します。

```text
BATTLE  第N戦 / 5
ENEMY   相手：敵名
TIME    残り秒数
SCORE   この戦闘中の暫定スコア
```

狭い画面では2列表示を維持します。

### 3.4 結果画面

結果画面は、総合結果と詳細レポートを分けます。

```text
総合結果
総合結果をシェア
詳細レポート
詳細レポートをコピー
もう一度遊ぶ
他のゲームで遊ぶ
```

「他のゲームで遊ぶ」は `GAME.labUrl` へ遷移します。

---

## 4. 連戦進行仕様

### 4.1 基本進行

最大5戦です。勝利した時だけ次の戦闘へ進めます。

```text
第1戦 勝利 → 報酬選択 → 第2戦
第2戦 勝利 → 報酬選択 → 第3戦
第3戦 勝利 → 報酬選択 → 第4戦
第4戦 勝利 → 報酬選択 → 第5戦
第5戦 終了 → 総合結果
```

### 4.2 結果ごとの扱い

| 1戦の結果 | 連戦上の扱い |
|---|---|
| 勝利 | 第1〜第4戦なら報酬選択へ進む。第5戦なら総合結果へ進む |
| 敗北 | その時点で総合結果へ進む |
| 引き分け | その時点で総合結果へ進む |
| リタイア | その時点で総合結果へ進む |

引き分けは続行不可です。

---

## 5. runState仕様

`state` は1戦の状態を管理します。`runState` は5人抜き全体の状態を管理します。

```js
const runState = {
  active: false,
  battleNo: 1,
  maxBattles: RUN_MAX_BATTLES,

  playerCarryParts: null,

  battleReports: [],
  rewardScoreTotal: 0,
  selectedRewards: [],

  wins: 0,
  finalOutcome: null,
  totalScore: 0,
  totalBattleScore: 0,
  settings: null,
  result: null
};
```

`runState.battleReports` には、各戦闘終了時の詳細記録を保存します。

---

## 6. 敵仕様

敵は固定順です。

| 戦闘番号 | 敵名 |
|---:|---|
| 第1戦 | 徘徊者 |
| 第2戦 | コメ職人 |
| 第3戦 | 荒らし |
| 第4戦 | モデレーター |
| 第5戦 | 尊厳の管理人 |

CPUの頭は、試合前設定で選んだ頭、またはランダム頭を使います。敵名は入力ではなく、戦闘番号で決まります。

CPUの部位状態は毎戦リセットします。

---

## 7. 部位仕様

### 7.1 部位一覧

ゲーム上の部位は次です。

```text
head
leftArm
rightArm
leftFoot
rightFoot
heart
```

心臓は急所です。心臓に攻撃が通ると、その戦闘は決着します。

### 7.2 耐久値

```js
const PART_MAX = Object.freeze({
  leftArm: 2,
  rightArm: 2,
  leftFoot: 2,
  rightFoot: 2,
  head: 3
});
```

腕と足は2回で壊れます。頭は3回で壊れます。頭が壊れた場合は、表示を `💩` に変えます。

---

## 8. 部位持ち越し仕様

### 8.1 持ち越すもの

プレイヤーだけ、次の部位状態を次戦へ持ち越します。

```text
head.damage / head.broken
leftArm.damage / leftArm.broken
rightArm.damage / rightArm.broken
leftFoot.damage / leftFoot.broken
rightFoot.damage / rightFoot.broken
```

### 8.2 持ち越さないもの

次の状態は持ち越しません。

```text
現在位置
向き
ジャンプ状態
しゃがみ状態
ガード状態
スタミナ残量
攻撃待ち時間
ヒット硬直
画面上の攻撃
画面上のエフェクト
CPUの部位状態
CPUの思考状態
```

第2戦以降は、通常の戦闘初期化を行った後、プレイヤーの部位状態だけを `runState.playerCarryParts` で上書きします。

---

## 9. 報酬仕様

### 9.1 報酬が出る条件

第1戦から第4戦で勝利した場合だけ、報酬選択画面を出します。

第5戦では勝利しても報酬選択を出しません。すぐ総合結果へ進みます。

### 9.2 報酬の種類

報酬は次のどちらかです。

```text
壊れている部位を応急処置（耐久1で復帰）
スコア +1000
```

### 9.3 回復対象

回復できるのは、次のうち `broken === true` の部位だけです。

```text
head
leftArm
rightArm
leftFoot
rightFoot
```

`damage > 0` でも、`broken === false` の部位は回復候補に出しません。

### 9.4 回復処理

応急処置は完全回復ではありません。ダメージを1だけ戻し、耐久1の状態で復帰させます。

```text
頭：damage 3 → 2、broken true → false
腕：damage 2 → 1、broken true → false
足：damage 2 → 1、broken true → false
```

壊れている部位がない場合は、スコア+1000だけ選べます。

---

## 10. 戦闘操作仕様

### 10.1 操作

```text
←        左移動
→        右移動
↑        ジャンプ
↓        しゃがみ
攻撃A    攻撃
ガードB  ガード
```

### 10.2 スタミナ

```text
通常攻撃: 1消費
頭・💩攻撃: 1.5消費
ガード: 発動時0.5消費、成功時の追加消費なし
```

スタミナは一定時間ごとに回復します。

---

## 11. 移動・ジャンプ・しゃがみ仕様

### 11.1 移動速度

```text
両足あり: 通常速度
片足なし: 0.5倍
両足なし: 0.3倍
```

両足が壊れていても移動できます。

### 11.2 ジャンプ

ジャンプには両足が必要です。

```text
両足あり: ジャンプ可能
片足なし: ジャンプ不可
両足なし: ジャンプ不可
```

### 11.3 しゃがみ

しゃがみは、足の状態に関係なく可能です。

---

## 12. 腕仕様

### 12.1 有効前面腕

相手に近い側の腕を前面腕とします。

前面腕が壊れていて、後ろ腕が残っている場合は、後ろ腕を前へ出します。この腕を有効前面腕とします。

```text
前面腕が生きている → 前面腕が有効前面腕
前面腕が壊れているが後ろ腕が生きている → 後ろ腕が有効前面腕
両腕が壊れている → 有効前面腕なし
```

有効前面腕は、表示、ガード、通常攻撃、被弾判定で共通して使います。

### 12.2 中段攻撃を受ける時

立ち状態で中段攻撃を受ける場合、命中先は次です。

```text
有効前面腕 → なければ心臓
```

両腕が壊れている場合、中段攻撃は心臓へ通ります。

---

## 13. 攻撃仕様

### 13.1 攻撃種別

攻撃は2種類です。

```text
fist: 腕が1本以上ある時の通常攻撃
head: 両腕が壊れている時の頭・💩攻撃
```

### 13.2 通常攻撃

腕が1本以上ある場合、攻撃ボタンで通常攻撃を出します。

```text
立ち: 中段
しゃがみ: 下段
ジャンプ: 上段
```

### 13.3 頭・💩攻撃

両腕が壊れている場合、攻撃ボタンで頭攻撃を出します。

```text
頭が壊れていない: 選択中の頭で攻撃
頭が壊れている: 💩で攻撃
```

頭・💩攻撃は地上専用です。ジャンプ中は出せません。しゃがみ中でも出せますが、判定は常に中段です。

両腕なしCPUは頭・💩攻撃しかできないため、攻撃直前ジャンプをしません。CPUのランダム思考内ジャンプも、両腕なしでは控えます。

```text
高さ: mid
スタミナ消費: 1.5
射程: LIMITS.fistRange - LIMITS.hitWidth
```

初期値では、通常攻撃射程が168、頭攻撃射程が122です。

頭・💩攻撃も攻撃回数に含めます。

---

## 14. 命中仕様

### 14.1 相手が立ち状態

```text
上段 → 頭
中段 → 有効前面腕。なければ心臓
下段 → 近い足。足がなければ空振り
```

### 14.2 相手がしゃがみ状態

```text
上段 → 空振り
中段 → 頭
下段 → 近い腕または足。なければ心臓
```

### 14.3 相手がジャンプ状態

```text
足があれば足
足がなければ空振り
```

---

## 15. ガード仕様

ガードには有効前面腕が必要です。

```text
中段ガード → 中段攻撃を防ぐ
下段ガード → 下段攻撃を防ぐ
上段攻撃 → ガードではなく、しゃがみ回避の対象
```

頭・💩攻撃は中段攻撃なので、中段ガードで防げます。

ガードで頭・💩攻撃を防いだ場合、攻撃側の頭にはダメージを入れません。頭にダメージが入るのは、拳攻撃または頭攻撃と相殺した場合だけです。

ガード中は、プレイヤーもCPUも攻撃・ジャンプ・移動ができません。しゃがみ姿勢を保つことはできます。

CPUが下段攻撃をガードする場合、`crouchUntil` を `guardMs` 分延長し、ガード時間中にしゃがみがすぐ解除されないようにします。

---

## 16. 攻撃相殺仕様

### 16.1 相殺対象

```text
fist vs fist: 相殺する
head vs fist: 相殺する
head vs head: 相殺する
```

### 16.2 相殺条件

次の条件を満たす場合、相殺します。

```text
攻撃者が違う
両方とも removed / resolved ではない
X座標差が28px以内
fist vs fist は height が同じ
head vs fist は head が mid、fist も mid
head vs head は両方とも mid
```

相殺後は両方の攻撃を消します。同じフレームで同じ攻撃が複数回相殺されないよう、相殺後はその組の処理を抜けます。

### 16.3 head vs fist の追加処理

頭・💩攻撃が拳攻撃と相殺した場合、頭攻撃を出した側に追加処理を行います。head vs head の場合は、双方の頭攻撃に同じ追加処理を行います。

#### 頭が壊れていない場合

頭に1ダメージを入れます。これで頭が壊れた場合、通常の頭破壊として集計します。

#### 頭が壊れている場合

💩攻撃が相殺された扱いです。

```text
プレイヤーの💩攻撃が相殺 → playerBrokenPartHits +1、playerPoopAttackClashes +1
CPUの💩攻撃が相殺 → enemyBrokenPartHits +1、cpuPoopAttackClashes +1
```

プレイヤーの💩攻撃が叩かれた場合、スコア内訳では「壊れた自分頭への追加被弾」として -300 相当になります。

CPUの💩攻撃を叩いた場合、スコア内訳では「壊れた敵頭への追加攻撃」として +300 相当になります。

💩攻撃が相殺された場合は、頭攻撃を出した側に短い無敵時間を付けます。すでに無敵時間中の場合、相殺した攻撃は消えますが、追加被弾回数と -300 / +300 相当のスコア用統計は重ねて加算しません。

---

## 17. 勝敗判定

心臓に攻撃が通った時点で、その戦闘は決着します。

```text
プレイヤーがCPUの心臓へ攻撃を通す → 勝利
CPUがプレイヤーの心臓へ攻撃を通す → 敗北
同一フレームで両方の心臓に攻撃が通る → 引き分け
リタイアボタンを押す → リタイア
制限時間が切れる → 引き分け
```

---

## 18. スコア仕様

### 18.1 1戦ごとのスコア

1戦ごとのスコア計算式は次です。

```text
勝利ボーナス: +5000
攻撃回数: -30 × attacks
ガード成功: +120 × guardSuccess
敵通常部位破壊: +300 × (enemyPartsBroken - enemyHeadBroken)
敵頭破壊: +1000 × enemyHeadBroken
壊れた敵頭への追加攻撃: +300 × enemyBrokenPartHits
自分部位破壊: -500 × playerPartsBroken
壊れた自分頭への追加被弾: -300 × playerBrokenPartHits
経過秒数: -10 × elapsedSec
```

この式は `calcScore()` と一致させます。

### 18.2 頭攻撃とスコア

頭・💩攻撃も `attacks` に含めます。したがって、1回ごとに -30 の対象です。

### 18.3 総合スコア

総合スコアは次です。

```text
総合スコア = 各戦闘スコア合計 + 報酬スコア加算合計
```

---

## 19. 戦闘レポート仕様

各戦闘終了時に、次の情報を保存します。

```js
const battleReport = {
  battleNo,
  playerName,
  playerHead,
  enemyName,
  enemyHead,

  outcome,
  outcomeLabel,

  battleScore,
  elapsedSec,

  attacks,
  headAttacks,
  guardSuccess,

  enemyPartsBroken,
  enemyHeadBroken,
  enemyBrokenPartHits,

  playerPartsBroken,
  playerHeadBroken,
  playerBrokenPartHits,

  playerHeadAttackClashes,
  cpuHeadAttackClashes,
  playerPoopAttackClashes,
  cpuPoopAttackClashes,

  scoreBreakdown,

  playerPartsBefore,
  playerPartsAfterBattle,
  reward,
  rewardScore,
  playerPartsAfterReward
};
```

---

## 20. 詳細レポート仕様

詳細レポートは、結果画面に表示し、コピーできるようにします。

各戦闘ごとに次を出します。

```text
第N戦: 敵名
結果
戦闘スコア
経過秒数
攻撃回数
うち頭攻撃回数
ガード成功回数
敵破壊数
敵頭破壊数
壊れた敵頭への追加攻撃回数
自分破壊数
自分頭破壊数
壊れた自分頭への追加被弾回数
自分の頭攻撃相殺回数
自分の💩攻撃を叩かれた回数
敵の頭攻撃を相殺した回数
敵の💩攻撃を叩いた回数
スコア内訳
戦闘前の状態
戦闘後の状態
報酬
報酬後の状態
```

最後に次を出します。

```text
戦闘スコア合計
報酬スコア加算
総合スコア
```

---

## 21. シェア・コピー仕様

### 21.1 総合結果シェア

総合結果シェアには、次を入れます。

```text
尊厳を賭けようか2 で遊んだ！

結果: 結果ラベル
総合スコア: n点
勝利数: x / 5
最終状態: 部位状態

GAME.url
```

### 21.2 詳細レポートコピー

「詳細レポートをコピー」ボタンで、詳細レポート全文をクリップボードへコピーします。

コピー成功時:

```text
詳細レポートをコピーしました
```

コピー失敗時:

```text
コピーできませんでした。下のテキストを手動でコピーしてください。
```

失敗時は、手動コピー用のテキスト欄を表示します。

---

## 22. ランキング送信仕様

ランキング送信は、総合結果時に1回だけ行います。各戦闘ごとには送信しません。

送信値は次です。

```text
p_display_name: プレイヤー名
p_game_slug: GAME.slug
p_score: 総合スコア
p_client_version: GAME.version
```

`GAME.slug` は必ず `songen_wo_kakeyouka2` です。

---

## 23. 固定解釈

この仕様では、次の解釈を固定します。

```text
回復できるのは broken === true の部位だけ
負傷中だが壊れていない部位は回復できない
第5戦後は報酬選択を出さない
引き分けは連戦終了
ランキング送信は総合スコアのみ
頭・💩攻撃は地上専用
頭・💩攻撃は常に中段
head vs head は相殺する
💩攻撃、頭攻撃、拳攻撃の相殺処理は、例外が発生してもゲームループを停止させない
相殺処理で例外が発生した場合、該当projectileを削除して戦闘を継続する
無敵時間中の頭・💩相殺では、追加ダメージや追加ペナルティを重ねない
head vs fist / head vs head の相殺処理は防御的に実装する
ガードで頭・💩攻撃を止めても、攻撃側の頭にダメージは入らない
💩攻撃を叩かれた時は、壊れた頭への追加被弾として扱う
CPUにも欠損時の継戦ロジックを適用する
```

---

## 24. 受け入れ条件

次を満たせば、この仕様の実装として成立します。

```text
ゲーム名、slug、URLが2用になっている
最大5戦の連戦として遊べる
勝利時のみ次戦へ進める
第1〜第4戦の勝利後に報酬選択が出る
第5戦後は報酬選択なしで総合結果へ進む
CPU名入力欄が存在しない
敵名が固定順で出る
プレイヤーの壊れた部位だけが次戦へ持ち越される
報酬で壊れた部位を応急処置（耐久1で復帰）できる
報酬でスコア+1000を選べる
前面腕欠損時に後ろ腕が前に出る
両足なしでも0.3倍速で移動できる
両腕なしで頭または💩攻撃が出る
頭・💩攻撃が拳攻撃と相殺する
head vs head は相殺する
💩攻撃が相殺された時に既存スコア要素へ反映される
1戦ごとのスコア計算式が仕様と一致する
総合スコアが正しく計算される
ランキング送信が総合スコアで1回だけ行われる
総合結果をシェアできる
詳細レポートをコピーできる
結果画面の「他のゲームで遊ぶ」が GAME.labUrl へ遷移する
スマホ幅でHUDの4項目が読める
```
